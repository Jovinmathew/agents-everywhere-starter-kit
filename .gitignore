node_modules/
.env
.env.local
*.log
dist/
.DS_Store
.turbo/
*.tsbuildinfo

# Local attempt/upload metadata (not task storage)
.data/

# Added by CopilotKit CLI: credential files, which must never be committed.
.env.*
!.env.example

# Added by CopilotKit CLI: managed Channel setup credentials and artifacts.
.copilotkit/artifacts/
