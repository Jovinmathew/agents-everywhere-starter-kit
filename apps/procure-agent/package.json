{
  "name": "procure-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Procurebot's agent: CopilotKit runtime with procurement tools over the shared Postgres.",
  "scripts": {
    "dev": "node --env-file=../../.env --import tsx --watch src/server.ts",
    "start": "node --env-file=../../.env --import tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@copilotkit/runtime": "1.70.3",
    "agent-core": "*",
    "procure-db": "*",
    "zod": "^4.1.0"
  }
}
